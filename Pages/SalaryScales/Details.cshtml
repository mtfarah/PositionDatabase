@page
@model PositionDatabase.Pages.SalaryScales.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>SalaryScale</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.SalaryScale.StartDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.SalaryScale.StartDate)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.SalaryScale.EndDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.SalaryScale.EndDate)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.SalaryScale.SalaryScaleId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
