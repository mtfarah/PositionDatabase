@page
@model PositionDatabase.Pages.SalaryScales.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SalaryScale[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalaryScale[0].EndDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SalaryScale) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SalaryScaleId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SalaryScaleId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SalaryScaleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
