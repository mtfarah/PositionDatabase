@page
@model PositionDatabase.Pages.SalaryScales.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>SalaryScale</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SalaryScale.SalaryScaleId" />
            <div class="form-group">
                <label asp-for="SalaryScale.StartDate" class="control-label"></label>
                <input asp-for="SalaryScale.StartDate" class="form-control" />
                <span asp-validation-for="SalaryScale.StartDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SalaryScale.EndDate" class="control-label"></label>
                <input asp-for="SalaryScale.EndDate" class="form-control" />
                <span asp-validation-for="SalaryScale.EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
